// Função chamada para mostrar informações sobre o elemento com o ID 'contact-list'
function show() {
    // Obtém a referência ao elemento HTML com o ID 'contact-list'
    const contactList = document.getElementById('contact-list');
    
    // Exibe no console a referência ao elemento obtido
    console.log(contactList);
}
