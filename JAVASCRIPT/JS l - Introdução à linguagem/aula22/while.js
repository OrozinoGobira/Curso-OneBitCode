let velocidade = 80


while (velocidade > 0){
    alert("o carro está a "+ velocidade + "km/h")
    velocidade -= 20 
    alert("Diminuindo 20km/h")
}

alert("O carro parou")

























/*// Inicializa a variável 'velocidade' com o valor 80
let velocidade = 80;

// Executa um loop while enquanto a velocidade for maior que 0
while (velocidade > 0) {
    // Exibe a velocidade atual do carro no alerta
    alert("Velocidade do carro: " + velocidade + " km/h");
    
    // Reduz a velocidade do carro em 20 km/h
    velocidade -= 20;

    // Exibe uma mensagem indicando que o carro diminuiu 20 km/h
    alert("O carro diminuiu 20 km");

    // O loop continua até que a velocidade seja menor ou igual a 0
}

// Exibe um alerta indicando que o carro parou após o loop while
alert("O carro parou");
*/