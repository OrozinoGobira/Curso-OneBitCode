function calcularMedia(a, b){
  const media = ( a + b) / 2
}

const  resultado = calcularMedia(7, 2)

console.log(resultado)

























/*// Função para calcular a média de dois números
function calcularMedia(a, b) {
  const media = (a + b) / 2;
  return media;
}

// Chamando a função calcularMedia com os argumentos 7 e 2
const resultado = calcularMedia(7, 2);
console.log(resultado); // Saída: 4.5

// Função para criar um objeto representando um produto
function criarProduto(nome, preco) {
  const produto = {
      nome,
      preco,
      estoque: 1
  };
  return produto;
}

// Chamando a função criarProduto com argumentos específicos e exibindo o resultado
console.log(criarProduto("Notebook intel core i3 8GB", 2500));

// Função para calcular a área de um retângulo
function areaRetangular(base, altura) {
  return base * altura;
}

// Função para calcular a área de um quadrado, que chama a função areaRetangular internamente
function areaQuadrada(lado) {
  return areaRetangular(lado, lado);
}

// Chamando as funções de cálculo de área e exibindo os resultados
console.log(areaRetangular(9, 4)); // Saída: 36
console.log(areaQuadrada(9));      // Saída: 81

// Função simples para imprimir "Olá, mundo" no console e retornar a string
function ola() {
  let texto = "---";
  texto = "Olá, mundo";
  console.log(texto);
  return texto;
}

// Chamando a função ola e exibindo o resultado
console.log(ola()); // Saída: Olá, mundo

// Função para verificar a maioridade com base na idade fornecida como argumento
function maioridade(idade) {
  if (idade >= 18) {
      return "Maior de idade";
  } else {
      return "Menor de idade";
  }
}

// Chamando a função maioridade com diferentes idades e exibindo os resultados
console.log(maioridade(12)); // Saída: Menor de idade
console.log(maioridade(22)); // Saída: Maior de idade*/